import { useEffect, useState } from "react";
import { apiClient } from "../../api/client";
export function AdminDashboardPage() { const [data,setData]=useState(null); useEffect(()=>{apiClient.get("/admin/dashboard").then(({data:response})=>setData(response));},[]); if(!data)return <div className="page-loading">Loading administration...</div>; return <section className="workspace"><header className="page-header"><div><p className="eyebrow">Administration</p><h2>Library Overview</h2></div></header><div className="stat-grid">{[["Total Users",data.totalUsers],["Pending Payments",data.pendingPayments],["Active Subscriptions",data.activeSubscriptions],["Total Seats",data.totalSeats],["Today's Entries",data.todayEntries]].map(([label,value])=><article key={label}><span>{label}</span><strong>{value}</strong></article>)}</div></section>; }
